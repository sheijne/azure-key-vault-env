{
  "name": "azkvenv",
  "description": "Preload environment variables from Azure Key Vaults",
  "version": "0.0.1",
  "type": "module",
  "bin": {
    "kv": "./bin/kv"
  },
  "files": [
    "./bin/kv"
  ],
  "exports": {
    "./package.json": "./package.json"
  },
  "scripts": {
    "build": "bun build --compile --outfile=./bin/kv ./src/bin.js",
    "prerelease": "bun run build",
    "release": "changeset publish",
    "prelink": "bun run build",
    "link": "bun link"
  },
  "dependencies": {
    "@clack/prompts": "^0.11.0",
    "citty": "^0.1.6",
    "dotenv": "^16.5.0",
    "pkg-types": "^2.1.0",
    "which": "^5.0.0"
  },
  "devDependencies": {
    "@changesets/cli": "^2.29.5",
    "@types/bun": "^1.2.17",
    "@types/which": "^3.0.4",
    "prettier": "^3.6.1"
  }
}
